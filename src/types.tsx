export interface TableType {
    id: number;
    name: string;
    age: number;
    address?: string;
    tags?: string[];
}

export interface FormType {
    name: string;
    age: number;
    address?: string;
    tags?: string[];
}

